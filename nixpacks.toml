# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = [
  "cd client && npm ci",
  "cd ../server && npm ci"
]

[phases.build]
cmds = [
  "cd client && npm run build"
]

[start]
cmd = "cd server && npm start"
