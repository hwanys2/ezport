# Portfolio Target Tracker

React + Express MVP for tracking target weights vs current weights with Yahoo Finance prices.

## Setup

### Backend

```bash
cd server
npm install
cp .env.example .env
npm run dev
```

### Frontend

```bash
cd client
npm install
cp .env.example .env
npm run dev
```

The frontend expects the API at `http://localhost:4000` by default.
